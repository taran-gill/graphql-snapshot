type Query {
    books: [Book!]!,
    authors(id: ID): [Author!]!,
    openForBusiness: Boolean!
}

type Book {
    name: String,
    author: Author,
    isbn: ID!
}

type Author {
    name: String
    id: ID!,,
    books: [Book!]!,
    birthplace: String
}
